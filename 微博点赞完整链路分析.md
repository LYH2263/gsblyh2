# 微博点赞完整链路分析

> 本文基于仓库真实代码梳理「用户点赞一条微博」的端到端链路，涉及后端 `like.py` / `post.py` / `notification.py` / `models/__init__.py` 与前端 `PostItem.vue` / `Discover.vue` / `api/index.js` / `router/index.js`。所有结论均对照源码，不凭接口名猜测。

---

## 一、点赞完整链路（正常路径）

### 1. 前端触发

用户在 [PostItem.vue](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L58-L67) 中点击心形按钮，触发 `toggleLike()`：

```js
async function toggleLike() {
  try {
    if (liked.value) {
      await likeAPI.unlikePost(props.post.id)
      likeCount.value--
    } else {
      await likeAPI.likePost(props.post.id)
      likeCount.value++
    }
    liked.value = !liked.value
  } catch (e) {
    ElMessage.error(e.message || '操作失败')
  }
}
```

请求经 [api/index.js](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/api/index.js#L77-L83) 的 `likeAPI.likePost` 发往 `POST /api/likes/post/:id`。请求拦截器自动附带 `Authorization: Bearer <token>`。

### 2. 登录态校验

后端 [like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L9-L16) 上的 `@jwt_required()` 装饰器先校验 JWT；解析出 `user_id` 后再查库确认用户存在（`用户不存在` → 404）。

### 3. 帖子存在性判断

[like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L18-L20) 用 `Post.query.filter_by(id=post_id).first()`，为空则返回 `帖子不存在` → 404。注意 `Post` 模型**没有软删除字段**（无 `is_deleted`），因此"帖子已被删除"与"帖子从未存在"在数据层是同一状态。

### 4. 重复点赞处理

[like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L22-L25) 先做应用层查询：若 `Like` 已存在则返回 `已点赞` → 400。同时 [models/__init__.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L131-L134) 上有数据库唯一约束 `unique_user_post_like (user_id, post_id)` 作为兜底。

### 5. 点赞入库

[like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L27-L31) 新建 `Like` 记录并 **第一次 commit**。

### 6. 向博主写入通知

[like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L33-L43)：仅当 `post.user_id != user_id`（即点赞者不是作者本人）时才构造 `Notification` 并 **第二次 commit**。成功后返回 `点赞成功` → 201。

### 7. 前端计数与状态刷新

- **乐观更新**：`toggleLike()` 在请求成功后本地 `likeCount++` / `liked=true`，不重新拉取。
- **初始状态**：[PostItem.vue](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L323-L326) 在 `onMounted` 调用 `fetchLikeStatus()`，请求 `GET /likes/post/:id/status`，由 [like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L69-L82) 返回 `liked` 与实时 `likes_count = post.likes.count()`。
- 也就是说，**列表首屏的每一个帖子都会各自发起一次独立的状态请求**。

---

## 二、四种情形下的后端响应与通知产生

### 情形 A：点赞者与作者为同一人（自赞）

- **后端响应**：链路完全走通，`Like` 落库成功，返回 `点赞成功` 201。
- **通知**：[like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L34) 的 `if post.user_id != user_id` 判断为假，**不写入通知**（不会给自己发"赞了你的微博"）。
- **与直觉的差异**：多数产品会**禁止自赞**或前端隐藏自赞后仍 +1。此处后端允许自赞并让 `likes_count +1`，直觉预期可能是"不允许自赞"或"自赞不计数"。当前实现是"允许自赞、计数 +1、只是不发通知"。

### 情形 B：帖子已被删除

- **后端响应**：由于帖子物理删除，`Post.query...first()` 返回 `None`，返回 `帖子不存在` → 404。
- **通知**：不产生。
- **与直觉的差异**：符合直觉（404）。但因为没有软删除，接口无法区分"曾经存在被删"与"ID 非法"，都归为 404。此外由 [models/__init__.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L75) 的 `cascade='all, delete-orphan'`，删帖会级联删除其 `Like`，但**历史已产生的点赞通知不会被回收**（`Notification.source_post_id` 只是可空外键，无级联清理），导致通知里可能残留指向已删帖子的死链。

### 情形 C：未登录

- **后端响应**：`@jwt_required()` 直接拦截，返回 401（缺少/无效 token）。
- **通知**：不产生。
- **与直觉的差异**：后端符合直觉。但前端存在**放大副作用**：[api/index.js](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/api/index.js#L30-L34) 的响应拦截器一旦收到 401，会清 `localStorage` 并强制 `window.location.href = '/login'`。而"发现页"路由 [router/index.js](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/router/index.js#L28-L39) 继承了父级 `meta.requiresAuth`，未登录根本进不去发现页。值得注意的是后端 [post.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/post.py#L182-L183) 的 `discover` 接口**没有 `@jwt_required`**（设计上想公开），但前端路由守卫却强制登录，形成"后端开放、前端封闭"的不一致。

### 情形 D：重复点赞

- **后端响应（正常时序）**：应用层 [like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L23-L25) 命中 `existing_like`，返回 `已点赞` → 400。
- **后端响应（并发时序）**：两个请求同时通过第 23 行的存在性检查，第二个 `commit` 会触发唯一约束 `unique_user_post_like` 冲突抛 `IntegrityError`，被 [like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L46-L48) 的宽泛 `except Exception` 捕获，返回 `点赞失败` → **500**。
- **通知**：正常重复点赞不产生（提前 400 返回）；并发失败分支也不产生。
- **与直觉的差异**：直觉预期重复点赞应**幂等或统一 400**，但并发下会返回 500（服务器错误语义），把"业务重复"误报成"系统故障"。

---

## 三、通知写入失败但点赞已提交：数据一致性

这是当前实现最实质的缺陷。[like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L29-L48) 把点赞和通知拆成了**两次独立 commit**：

```python
db.session.add(like)
db.session.commit()          # ← 第一次提交：点赞已经落库、不可回滚

if post.user_id != user_id:
    notification = Notification(...)
    db.session.add(notification)
    db.session.commit()      # ← 第二次提交：若此处失败……
```

若第二次 commit 失败（DB 抖动、连接断开、约束异常等）：

1. **点赞已经持久化**，`likes_count` 已经 +1，无法回滚——第 47 行的 `db.session.rollback()` 只能回滚"当前未提交事务"，即通知，回滚不了已提交的点赞。
2. **返回给前端 500**，前端 `toggleLike` 进入 `catch`，`likeCount++` 不会执行，`liked` 保持 `false`。
3. 结果：**数据库里点赞真实存在，但用户界面显示未点赞、计数未变；博主也收不到通知**。用户重试点赞时，又会命中"已点赞 → 400"，界面进一步困惑。

**根因**：本该是一个原子事务的两个写操作被切成两段。正确做法是把 `Like` 和 `Notification` 放进**同一个事务、一次 commit**（通知失败则整体回滚，或将通知降级为非阻塞的后置任务），使"点赞成功"与"计数/通知"保持一致语义。

---

## 四、列表页嵌套查询点赞数的性能与竞态风险

### 1. N+1 查询（性能）

时间线 [post.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/post.py#L158-L179) 与发现页 [post.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/post.py#L182-L196) 都执行 `[p.to_dict() for p in posts.items]`。而 [models/__init__.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L77-L90) 的 `Post.to_dict()` 内部：

```python
'likes_count': self.likes.count(),
'comments_count': self.comments.count()
```

`likes` / `comments` 都是 `lazy='dynamic'` 关系，每次 `.count()` 都会发一条独立 SQL。此外 `data['author'] = self.author.to_dict()` 里 [User.to_dict()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L54-L56) 又各查了 `followers_count`、`following_count`、`posts_count`。

**结果**：一页 20 条帖子 ≈ `1（帖子列表） + 20×（likes count + comments count + author 的 3 次 count）= 1 + 20×5 ≈ 101 条 SQL`。分页越深、单页越大，数据库压力线性放大。应改为 `JOIN + GROUP BY` 聚合，或用计数缓存字段。

### 2. 前端并发请求风暴（性能）

首屏渲染时，[PostItem.vue](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L323-L326) 每个卡片 `onMounted` 各发 1 次 `fetchLikeStatus()`（+1 次 `fetchFollowStatus()`）。20 条帖子瞬间打出 **40 个并发请求**，且 `getPostLikeStatus` 里 `post.likes.count()` 又各触发一次 DB count。列表本已在 `to_dict()` 返回了 `likes_count`，前端却重复拉取，属于冗余往返。

### 3. 竞态与计数不一致（正确性）

- **列表快照 vs 逐条状态**：`to_dict()` 返回的 `likes_count` 与随后每张卡片 `fetchLikeStatus()` 返回的 `likes_count` 是**两个时间点的两次查询**，中间若有他人点赞，前端会以后者覆盖，出现同一屏内计数跳变。
- **乐观更新漂移**：`toggleLike()` 成功后只做本地 `++/--`，不与服务器对账。若期间该帖被并发点赞/取消，本地 `likeCount` 会与真实值偏离，直到下次 `fetchLikeStatus` 才纠正。
- **自赞计数**：因情形 A 允许自赞，作者刷新自己主页时会看到自己给自己贡献的点赞数，与"点赞数=他人认可数"的直觉不符。

---

| 场景 | 当前行为 | 潜在风险 |
| --- | --- | --- |
| 点赞者=作者（自赞） | `Like` 落库、`likes_count +1`、返回 201；`post.user_id != user_id` 为假故**不发通知** | 允许自赞刷计数，与"禁止自赞/自赞不计数"的直觉预期不符 |
| 帖子已被删除 | 物理删除后查不到，返回 404「帖子不存在」；无软删除字段 | 无法区分"被删"与"非法 ID"；级联删 `Like` 但历史点赞通知残留死链 |
| 未登录 | 后端 `@jwt_required` 返回 401；前端 401 拦截器清 token 并跳转 `/login` | 后端 `discover` 本是公开接口，前端路由守卫却强制登录，前后端开放策略不一致 |
| 重复点赞 | 正常时序返回 400「已点赞」；并发时唯一约束冲突被宽泛 `except` 捕获 → 返回 500 | 业务重复被误报为系统错误 500，非幂等；违背幂等/统一 400 的直觉 |
| 通知写入失败但点赞已提交 | 点赞与通知**分两次 commit**，第二次失败仅回滚通知，返回 500 | 点赞已持久化但前端显示未点赞、计数不变、博主无通知，数据与 UI 双重不一致 |
| 列表嵌套查询点赞数 | `to_dict()` 对每条帖子及作者各自 `.count()`；前端每卡片再单独 `fetchLikeStatus` | N+1 查询（约 101 条 SQL/页）+ 首屏 40 并发请求；两次查询快照差导致计数跳变与乐观更新漂移 |
