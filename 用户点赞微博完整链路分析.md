# 用户点赞一条微博：完整链路梳理

> 本文件基于仓库当前实际代码（后端 Flask + SQLAlchemy，前端 Vue 3 + Axios）梳理，所有结论均可回溯到具体文件与行号。

## 1. 参与方与代码入口

| 层 | 关键文件 |
|---|---|
| 前端 API 封装 | [index.js](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/api/index.js) |
| 前端列表卡片 | [PostItem.vue](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue) |
| 前端时间线/发现页 | [Timeline.vue](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/views/Timeline.vue)、[Discover.vue](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/views/Discover.vue) |
| 后端点赞路由 | [like.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py) |
| 后端帖子路由 | [post.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/post.py) |
| 后端通知路由 | [notification.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/notification.py) |
| ORM 模型 | [__init__.py](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py) |
| 蓝图/JWT 注册 | [create_app()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/__init__.py#L11-L67) |

点赞相关的 HTTP 路由前缀为 `/api/likes`（见 [__init__.py#29](file:///d:/Asolo4/众测723/gsblyh2/backend/app/__init__.py#L29)），核心写接口是 `POST /api/likes/post/<post_id>`，处理函数为 [like_post()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L9-L48)。

## 2. 一次「点赞」请求经过的链路

### 2.1 前端：点击心形按钮到拿到响应

1. [PostItem.vue#L59-L62](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L59-L62) 的心形图标绑定了 `@click="toggleLike"`，图标根据本地 `liked` 状态在 ❤️/🤍 间切换，数字显示本地的 `likeCount`。
2. [toggleLike()](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L260-L273) 根据当前 `liked.value` 选择调用 `likeAPI.likePost(postId)` 或 `likeAPI.unlikePost(postId)`，**等 `await` 成功后**才更新本地的 `liked` 与 `likeCount`（保守式更新，非乐观更新）。
3. [likeAPI.likePost()](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/api/index.js#L77-L83) 实际发 `POST /api/likes/post/{id}`。
4. Axios 请求拦截器（[index.js#L9-L20](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/api/index.js#L9-L20)）自动在请求头里塞入 `Authorization: Bearer <localStorage.token>`；响应拦截器（[index.js#L23-L39](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/api/index.js#L23-L39)）把 401 统一清理 token 并跳转到 `/login`，其它错误码把后端 `error` 字段弹成 `ElMessage.error`。

### 2.2 后端：从 JWT 到落库与通知

`like_post()` 的步骤按顺序为：

1. **登录态校验**：函数装饰器 [@jwt_required()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L10) 先于函数体执行。没有 `Authorization` 头 / token 失效 / token 伪造都由 Flask-JWT-Extended 直接返回 401，函数体不会执行。
2. **取用户、判用户存在**：`user_id = int(get_jwt_identity())` 后 `User.query.filter_by(id=user_id).first()`，若用户记录已被删除则返回 `404 {'error':'用户不存在'}`（[like.py#L12-L16](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L12-L16)）。
3. **判帖子存在**：`Post.query.filter_by(id=post_id).first()`，未找到则返回 `404 {'error':'帖子不存在'}`（[like.py#L18-L20](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L18-L20)）。
4. **重复点赞检查**：`Like.query.filter_by(user_id=user_id, post_id=post_id).first()`，命中则返回 `400 {'error':'已点赞'}`（[like.py#L22-L25](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L22-L25)）。注意这是普通 SELECT，与后续 INSERT **不在同一个加锁区间**。
5. **入库**：`db.session.add(like)` 后第一次 `db.session.commit()`（[like.py#L27-L31](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L27-L31)）。若违反 [UniqueConstraint('user_id','post_id')](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L131-L134) 或其它数据库错误，抛异常进入 `except`，rollback 后返回 500。
6. **通知**：若 `post.user_id != user_id`，构造 `Notification(type='like', source_post_id=post_id, source_user_id=user_id, ...)`，再 `db.session.add` + **第二次** `db.session.commit()`（[like.py#L34-L43](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L34-L43)）。若 `post.user_id == user_id`，直接跳过通知逻辑。
7. **返回**：通知提交成功后返回 `201 {'message':'点赞成功'}`。通知写入失败时同样落入统一 `except`，返回 `500 {'error':'点赞失败'}`。

### 2.3 列表渲染与计数刷新

- 时间线与发现页分别调用 [`POST /api/posts/timeline`](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/post.py#L158-L179)（需登录）与 [`GET /api/posts/discover`](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/post.py#L182-L196)（无需登录）。两者都把 `Post.to_dict()` 的结果直接序列化下发。
- [Post.to_dict()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L77-L90) 内联计算 `likes_count: self.likes.count()` 与 `comments_count: self.comments.count()`，还会调用 `self.author.to_dict()`，而后者再做 `followers.count() / following.count() / posts_count` 三个 count。
- 前端 `PostItem` 初始化时（[PostItem.vue#L164-L165](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L164-L165)）`liked=false`、`likeCount=props.post.likes_count || 0`。`onMounted` 再调用 [`GET /api/likes/post/<id>/status`](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L69-L82) 把 `liked` 和 `likes_count` 以"该用户的真实状态"覆盖一次（[PostItem.vue#L220-L228](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L220-L228)）。
- 点赞/取消赞成功后只改本地的 `liked` 与 `likeCount`，**不重新拉取整列**，也不重新调用 status 接口。

## 3. 四种异常场景下的后端响应与通知

| 场景 | 走到哪一步 | HTTP 响应 | 是否写 Like | 是否写 Notification | 备注 |
|---|---|---|---|---|---|
| **点赞者即作者** | 通过登录、用户、帖子、重复检查；第一次 commit 成功；第 34 行 `if post.user_id != user_id` 判等为假 | `201 {'message':'点赞成功'}` | 写入 | **不写** | 自赞的记录会保留在 `likes` 表里，计数也会自增 1，只是不给自己发通知。 |
| **帖子已被删除** | Post 模型无 `is_deleted` 字段，`delete_post` 走 `db.session.delete(post)` 硬删除（[post.py#L135-L155](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/post.py#L135-L155)）。点赞流程在第 3 步 `if not post` 命中 | `404 {'error':'帖子不存在'}` | 不写 | 不写 | 无软删除语义，"已删"等价于"不存在"。由于 `Like.post_id` 外键带 `cascade='all, delete-orphan'`，删帖会顺带把历史点赞一并物理删除。 |
| **未登录** | `@jwt_required()` 在函数执行前被 Flask-JWT-Extended 拦截 | 默认 `401`（由扩展生成，body 为 `{'msg':'Missing Authorization Header'}` 或类似） | 不写 | 不写 | 前端响应拦截器遇 401 会清 token 并跳 `/login`（[index.js#L30-L34](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/api/index.js#L30-L34)）。 |
| **重复点赞** | 第 4 步 `existing_like` 命中 | `400 {'error':'已点赞'}` | 不写 | 不写 | 此为顺序执行的「先查后插」，**仅单请求场景下成立**，并发情况见第 5 节。 |

### 与直觉预期的差异

1. **自赞也会落库、也会让点赞数 +1**。许多产品（如微博、Twitter）禁止给自己点赞，或自赞不计数；这里允许并且计数，只是不产生通知。如果产品预期"自己不算赞"，当前实现与预期不一致。
2. **"帖子已删"不是一个独立状态**。后端无法区分"从未存在"和"已被删除"，对客户端都返回 404。如果产品想展示"该微博已被删除"的占位，需要 `is_deleted` 软删字段，目前没有。
3. **未登录用户在前端其实无法停留在发现页**。`/posts/discover` 本身不需要登录，但 `PostItem.onMounted` 无条件调用 [getPostLikeStatus](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L69-L82)，而该接口也被 `@jwt_required()` 保护，未登录时返回 401，被全局响应拦截器强制跳转 `/login`。这与"发现页可匿名浏览"的直觉不符。
4. **重复点赞的 400 只在串行下稳定**，并发下会被打成 500（详见第 5 节）。
5. **点赞失败 500 的文案是 `点赞失败`，但实际上 like 可能已经落库**（见第 4 节），文案与真实结果可能相反。
6. [PostItem.vue#L230-L238](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L230-L238) 中 `fetchFollowStatus` 写的是 `followAPI.getFollowStatus(post.author.id)`，`post` 并未在 `<script setup>` 中定义（只有 `props.post`），首次渲染非本人微博时会抛 `ReferenceError: post is not defined`，导致关注按钮状态获取不到。这与点赞链路本身无关，但同卡片内会影响用户操作。

## 4. 通知写入失败但点赞已提交：数据一致性分析

核心问题在 [like.py#L29-L48](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L29-L48) 用了**两次 `commit()`**：

```python
db.session.add(like)
db.session.commit()          # ① Like 已被事务 A 持久化

if post.user_id != user_id:
    notification = Notification(...)
    db.session.add(notification)
    db.session.commit()      # ② 若此处抛异常
...
except Exception as e:
    db.session.rollback()    # ③ 只能回滚"当前事务"中未提交的部分
    return jsonify({'error': '点赞失败'}), 500
```

数据库语义决定了：

- 一旦 ① 处 `commit()` 成功返回，`likes` 表的新行就对其它事务可见（受事务隔离级别影响，但已落盘）。
- 若 ② 处因 `notifications` 表约束冲突、连接断开、死锁、字段长度溢出等抛出异常，控制权进入 `except`，`session.rollback()` 只能回滚"当前事务里尚未提交的 Notification 对象"，**无法撤销已经在 ① 提交的 Like**。
- 因此最终态是：**Like 记录已落库，但 Notification 丢失，接口却返回 500 和"点赞失败"**。

后果：

- 博主侧：赞数 +1，但通知中心看不到这条"XX 赞了你的微博"，博主不会感知；后续即使作者去看点赞列表也看不到"谁赞的"之外的通知痕迹。
- 点赞者侧：看到 `ElMessage.error('点赞失败')`，按钮状态由于 [toggleLike](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L260-L273) 只有在 await 成功时才翻转，本地仍显示未赞；用户再点一次就会命中"已点赞"400，体验错乱；刷新页面后 `fetchLikeStatus` 又会显示已赞，计数也会 +1。
- 没有任何补偿机制（没有 outbox 表、没有重试队列、没有日志告警、没有把两次写入合并到单事务）。

可以考虑的修复方向（仅指出，不在本次范围内修改）：

- 把 Like 与 Notification 在**同一个事务**里 `add` 再 `commit` 一次，让数据库原子化保证"要么都成功要么都回滚"。
- 或采用"先提交 Like，再用消息/任务队列异步投递通知"的 outbox 模式，并对通知写入失败做重试与告警。
- 至少应当区分"点赞写入失败"和"通知写入但响应失败"两类错误码/文案，避免误导客户端。

## 5. 时间线/发现页嵌套查询点赞数：性能与竞态风险

### 5.1 N+1 查询

`/posts/timeline` 与 `/posts/discover` 一次性返回一页 20 条（默认 `per_page=20`），每条都走 [Post.to_dict()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L77-L90)。该方法对每条帖子触发：

- `self.likes.count()` → 1 条 COUNT
- `self.comments.count()` → 1 条 COUNT
- `self.author.to_dict()` → 内部 3 条 COUNT（`followers.count()` / `following.count()` / `posts.count()`，见 [User.to_dict()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L43-L57)）

即每条帖子 5 条 COUNT，20 条 = **100 条 COUNT 查询**，再加上拉取 Post/User 本体的若干条，一页请求触发上百条 SQL。`per_page` 越大倍数越高。所有关系都声明为 `lazy='dynamic'`（见 [Post.likes / Post.comments](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L74-L75)），意味着 `.count()` 真的会去打数据库，而不会走 SQLAlchemy 的动态列表缓存。

优化方向通常是：在列表查询里用 `db.session.query(Post, func.count(Like.id)).outerjoin(...) ... group_by(Post.id)` 做聚合，或者在 Post 表里冗余 `likes_count` 字段，点赞/取消赞时用原子 `UPDATE posts SET likes_count = likes_count ± 1` 维护。

### 5.2 "按热度排序"名实不符

[get_discover()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/post.py#L182-L196) 的注释写"按点赞数和评论数排序"，实际 SQL 是 `Post.query.order_by(Post.created_at.desc())`，完全是按时间倒序。真实的热度排序需要 join likes 并 `GROUP BY post.id ORDER BY COUNT(*) DESC`，当前实现达不到产品宣称的效果。

### 5.3 重复点赞的竞态：唯一约束 vs 400/500

[like_post()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L22-L31) 的检查是：

```python
existing_like = Like.query.filter_by(user_id=user_id, post_id=post_id).first()
if existing_like:
    return jsonify({'error': '已点赞'}), 400
like = Like(user_id=user_id, post_id=post_id)
db.session.add(like)
db.session.commit()
```

这是典型的「check-then-act」非原子序列。并发场景：

- 用户双击按钮、前端在弱网下重发、或者两个客户端同时点赞；
- 事务 T1 与 T2 几乎同时执行 SELECT，都返回 `None`；
- T1 先 INSERT 并 commit，成功；
- T2 再 INSERT，撞上 [`unique_user_post_like`](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L132) 唯一约束，抛 `IntegrityError`；
- 该异常被 [except Exception](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L46) 一把兜住，rollback 后返回 `500 {'error':'点赞失败'}`。

也就是说：**"重复点赞"在并发下不会返回 400，而是 500**。这与第 3 节表里串行场景的结论不一致。前端 [toggleLike](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L260-L273) 未做按钮禁用/请求去抖，连点会放大这个窗口。

正确做法：

- 直接尝试 INSERT 并捕获 `IntegrityError`/`sqlalchemy.exc.IntegrityError`，识别出唯一约束冲突时返回 400/409「已点赞」；
- 或在数据库层用 `INSERT ... ON CONFLICT DO NOTHING` / `INSERT IGNORE`，再按影响行数判断；
- 前端在请求 in-flight 期间禁用心形按钮或用 `liked` 之外的 `pending` 状态互斥。

### 5.4 列表计数与 status 接口之间的读偏差

- 列表接口里的 `likes_count` 来自 `to_dict()` 在序列化那一刻的 `SELECT COUNT(*)`；
- 而 [get_post_like_status()](file:///d:/Asolo4/众测723/gsblyh2/backend/app/routes/like.py#L69-L82) 返回的 `likes_count` 是在 `onMounted` 之后、几百毫秒到几秒后再 COUNT 一次。
- 两次 COUNT 之间若有其他用户点赞/取消，前端会出现"列表里显示 5，组件挂载后突然变 6"的瞬时跳变。
- 更微妙的是 `get_post_like_status` 里的顺序：先 `Like.query.filter_by(user_id, post_id).first()` 判断我是否赞过，再 `Post.query.get(post_id)` 判存在。如果这之间帖子被作者硬删（`delete_post` 通过 `cascade='all, delete-orphan'` 连带删 like），由于 `likes` 关系上配置了 cascade，删帖行为会把 like 也带走，于是 status 接口对该用户会回 404，即使列表数据里这条帖子还在（因为列表是之前查的）。这是删帖与列表渲染之间的一个可见竞态。

### 5.5 通知的悬挂外键

`Notification.source_post_id` 为普通 `db.ForeignKey('posts.id')`，未声明 `ondelete` 级联（[Notification 模型](file:///d:/Asolo4/众测723/gsblyh2/backend/app/models/__init__.py#L150-L179)）。而 `Post` 被硬删时，`Like` 通过 `Post.likes = relationship(... cascade='all, delete-orphan')` 一起删除，**Notification 并未挂在 Post 的关系下**。在 MySQL/InnoDB 默认外键策略下，删帖会因 notifications 表里还引用着 post_id 而失败，或在 SQLite/未启用 FK 约束的环境下留下指向不存在 post_id 的通知。无论哪种，都与"点赞通知"链路的最终一致性预期不符。

### 5.6 前端快速连点的本地竞态

[toggleLike](file:///d:/Asolo4/众测723/gsblyh2/frontend/src/components/PostItem.vue#L260-L273) 没有 `inFlight` 标志：

```js
if (liked.value) {
  await likeAPI.unlikePost(...)
  likeCount.value--
} else {
  await likeAPI.likePost(...)
  likeCount.value++
}
liked.value = !liked.value
```

用户在第一个请求尚未 resolve 时再次点击，`liked.value` 仍是旧值，会发出与第一次同方向的第二次请求。后端唯一约束在 POST 分支会兜底为 500，DELETE 分支则因为 like 已被第一次请求删掉而走 `if not like: return 404`，这些都会变成 toast 错误，但本地状态要等所有请求都结束后才稳定，容易出现"点一下，闪两次"的视觉跳变。

## 6. 汇总表

| 场景 | 当前行为 | 潜在风险 |
|---|---|---|
| 登录态校验 | `@jwt_required()` 统一拦截，缺失/失效由 Flask-JWT-Extended 返回 401；前端拦截器清 token 跳 `/login` | 前端所有点赞相关接口（含 status）共用 401 逻辑，未登录浏览发现页时 `PostItem` 挂载即触发 status 接口 → 401 → 被强制踢回登录页，匿名浏览体验被破坏 |
| 用户存在性校验 | token 解析出 user_id 后再 `User.query.first()`，无用户返回 404 | 纯冗余检查；若用户被删但 token 未过期，点赞被拒但本地 token 仍有效，前端要等 401 才会跳登录，状态不同步 |
| 帖子存在性判断 | `Post.query.filter_by(id=...).first()`，未命中 404；无软删除 | "帖子已删"与"帖子不存在"不可区分；硬删除依赖 `cascade='all, delete-orphan'` 级联删 Like，但 Notification 无此关系，会产生外键冲突或悬挂引用 |
| 重复点赞（串行） | 先 SELECT existing_like，命中返回 400「已点赞」 | "check-then-act" 非原子；前端无请求去抖/按钮禁用，用户连点易复现并发路径 |
| 重复点赞（并发） | 两事务都通过 SELECT 检查，第二个 INSERT 撞唯一约束，被 `except Exception` 兜成 500「点赞失败」 | 用户看到的是服务器错误而非"已点赞"；无法从 500 区分"真的挂了"还是"点快了"；监控上会出现大量误导性 5xx |
| 点赞入库 | 第一次 `db.session.commit()` 立即持久化 Like | Like 与 Notification 被拆成两次事务，无法同生共死，见下方"通知失败"一行 |
| 通知写入（点赞者≠作者） | 构造 Notification，第二次 `commit()` 成功后返回 201 | 见下一行；通知内容里 `source_post_id` 外键无 `ondelete` 策略，与硬删帖耦合 |
| 通知写入（点赞者=作者） | 跳过通知直接 201 | 自赞会落库并 +1 计数，但不发通知；若产品预期"自赞不计入"，当前会让点赞数虚高 |
| 通知写入失败 | 异常进 `except`，`rollback()` 只回滚未提交事务；返回 500「点赞失败」 | **Like 已落库而通知丢失**；客户端按失败处理，但刷新后会看到已赞、计数已增，出现"失败又成功"的错觉；无 outbox/重试/告警 |
| 前端按钮计数刷新 | `onMounted` 调 `/likes/post/<id>/status` 覆盖初始值；点赞/取消成功后本地 `liked`/`likeCount` 自增自减，不重新拉列表 | 首屏闪烁（先 props 计数后真实状态）；本地与其他客户端的实时变化不一致；快速连点存在 in-flight 竞态，可重复发同方向请求 |
| 列表计数 N+1 | `Post.to_dict()` 对每条帖子做 likes/comments count 并连带 author.to_dict() 里 3 个 count | 一页 20 条约触发 100+ 条 COUNT，`per_page` 调大时线性恶化；无聚合/无冗余计数字段 |
| 发现页排序 | 注释声称按点赞+评论热度排序，实际按 `created_at desc` | 产品预期与代码不符，"热门"名实不符；未来若真做热度排序还需要避免上述 N+1 |
| 列表计数与 status 计数读偏差 | 列表 COUNT 与卡片挂载后 status COUNT 是两次独立查询 | 高并发下数字会在挂载瞬间跳变；删帖在两次查询之间发生时 status 直接 404，而卡片还在页面上 |
| 通知外键一致性 | `Notification.source_post_id` 普通外键，Post 删除时 Like 级联删但 Notification 不级联 | InnoDB 默认 RESTRICT 可能让删帖报错；非 FK 强制环境下留下指向不存在帖子的"僵尸通知"，作者点进去 404 |
